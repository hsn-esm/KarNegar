<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        تنها مجوز اپ. فقط برای خواندن تاریخ تعطیلات رسمی از تقویم آنلاین
        استفاده می‌شود. هیچ داده‌ای از کاربر (نام، شیفت‌ها، کارکرد) ارسال نمی‌شود
        و اپ بدون اینترنت هم با تقویم درون‌برنامه‌ای کار می‌کند.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        ذخیره‌ی گزارش در پوشه‌ی عمومی «Download».
        روی اندروید ۱۰ و بالاتر از MediaStore استفاده می‌شود که مجوز نمی‌خواهد،
        پس این مجوز با maxSdkVersion به نسخه‌های ۷ تا ۹ محدود شده است.
    -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.KarNegar"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.KarNegar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
